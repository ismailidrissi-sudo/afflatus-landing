# Do not serve user hashes over HTTP (LiteSpeed / Apache)
<Files "users.bootstrap.json">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</Files>
